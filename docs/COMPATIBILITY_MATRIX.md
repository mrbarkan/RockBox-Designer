# Compatibility Matrix

This matrix records separate evidence levels. It deliberately does not combine them into a single compatibility percentage.

| Area | Preserved | Parsed | Interpreted | Rendered | Editable | Officially validated | Baseline note |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Generated official tag names | Yes | 193 names and longest boundaries | No blanket claim | No blanket claim | No blanket claim | No | Names, raw parameter specs, flags, tokens, and categories come from the pinned Rockbox tag table. |
| iPod Video device profile | Profile data preserved | Target and capabilities loaded | Feature gates | Native 320×240 canvas | Profile selection | Source-config verified | `ipodvideo` values match the pinned config and local verifier. |
| iPod Classic device profile | Profile data preserved | Target and capabilities loaded | Feature gates | Native 320×240 canvas | Profile selection | Source-config verified | `ipod6g` has no configured tuner, so FMS is hidden despite identical LCD dimensions. |
| Official parser fixture bridge | Browser source preserved for all 6 | Browser structural results recorded | Not applicable | Not applicable | Not applicable | Yes, target-specific `checkwps` | Three accepted by both, one intentional future-tag difference, one diagnostic difference, and one target-dependent result. |
| Phase 4 tag/device dashboard | Yes for all 193 registered names on both profiles | 386 tag/device rows | 121 evidenced interpreter tags; no blanket claim | 121 evidenced preview tags; no blanket claim | 12 user-facing source-aware tag surfaces | 13 Video and 11 Classic tags have accepted fixture/pixel evidence | The Compatibility Lab exposes each state separately and marks Classic radio plus non-touch target limitations without deleting or downgrading source preservation. |
| Phase 5 device-state simulator | Source remains authoritative | Named scenarios and actions are typed | Playback, seek, track, metadata, power, USB, hold, RTC, RTL, FM/RDS, touch-capability, and remote-capability state | Existing native-pixel renderer | State controls only; no source rewrite | State meanings source-inspected at pinned SHA | Presets are deterministic and shareable; target gates prevent Classic FM and current-profile touch/remote simulation. |
| Phase 6 component ecosystem | Existing bytes remain exact around insertions | 19 versioned component contracts and stable CST instance boundaries | Source templates declare required tags/capabilities | Existing source-linked renderer | Property-driven insert and exact safe remove with undo | 53/53 available target/screen CheckWPS fixtures accepted | Handles, viewport names, asset paths, shared references, unsupported screens, and target gates are explicit; touch remains unavailable on both current profiles. |
| Phase 7 full simulator feasibility | Browser source remains authoritative and independent | Native architecture inspected at pinned SHA | Level C firmware semantics remain external | One native iPod Video target emits reproducible firmware frames | Not a browser edit surface | Native core, authored-theme load, and two clean captures evidenced | Phase 7 passes by documented blockers: no GPL WebAssembly runtime is shipped; build, threads, main loop, dynamic code, filesystem, audio, bundle, and maintenance decisions remain explicit. |
| Phase 8 Firmware Assets USB fallback | Theme documents remain unchanged | Exact target, SHA, BMP header, and package manifest | Compiled fallback placement maps to a generated source macro | Placement preview only; external Rockbox remains authoritative | Opt-in fallback logo and left/center/right placement | Patch applies; two complete `ipodvideo` builds produce identical firmware images | SBS owns the themeable scene. Changing the compiled fallback requires custom firmware, backup, and recovery acknowledgement. |
| Phase 4 canonical simulator comparison | Authored SBS source retained | `%V`, `%Vf`, `%Vb`, `%VI`, `%Vi` | Named firmware UI viewport | Browser RGB vs official simulator framebuffer | Not an edit workflow | Two identical clean official captures | 6,315/76,800 pixels differ, all classified as native text/row layout or selector approximation; zero pixels remain unclassified. |
| Real-theme corpus | Exact for local AMusicPod and Adwaitapod | Zero browser syntax/package diagnostics in the latest local run | Reported per tag; no blanket claim | Reported per tag; no blanket claim | Reported per tag; no blanket claim | Adwaitapod accepted; AMusicPod source rejected at documented line | Private ZIPs remain ignored; package manifests and asset hashes survive export/re-import. |
| Phase 1A syntax document | Yes for tested corpus | Structural subset | No | No | Serializer primitive only | No | Twenty named fixtures and randomized safe-fragment combinations round-trip exactly. |
| WPS product workflow | Yes for untouched and tested edits | Structural subset | Documented Phase 2 subset | Native-pixel operation list | Viewport, text, image, bars, colors, touch, branch preview | Edited export accepted for Authored Full | AMusicPod and Adwaitapod complete minimum-change visual edit/export/re-import locally. |
| SBS product workflow | Yes for untouched and tested edits | Structural subset | Current-screen/UI viewport, menu/list, quick-screen, selector, scrollbar, iconset subset | Shared native-pixel operation list | Source-linked viewport and known property/source edits | Edited export accepted for Authored Full | Adwaitapod completes WPS/SBS/FMS import, edit, export, and re-import; firmware rows remain derived rather than authored elements. |
| FMS product workflow | Yes for untouched and tested edits | Structural subset | Frequency, preset, signal, stereo, tuned/scan, and RDS subset | Shared native-pixel operation list | Source-linked viewport and known property/source edits | Edited export accepted for Authored Full | Unsupported tuner tags remain source-exact and visible. |
| Quick screen and USB | SBS source remains exact | Activity states 10 and 21 recognized | Firmware-controlled quick-screen projection; theme-authored USB SBS branches plus compiled fallback boundary | Quick-screen and USB scene simulation in SBS viewports; Firmware Assets placement preview for the fallback | SBS source/property edits; fallback package only in opt-in Firmware Assets | Adwaitapod USB branch accepted by CheckWPS; fallback patch applies and full iPod Video target builds twice | No `.qs` or `.usb` source file is invented. Adwaitapod's 1 × 1 `%Vi` clips the fallback exactly. |
| Rockbox `.fnt` assets | Exact binary bytes | RB12 header and metrics | Height, ascent, maximum width, range, glyph count | Browser glyph raster remains approximate | Existing `.fnt` import plus local-helper TTF/OTF/TTC conversion with size/range controls | Native output loaded in current Rockbox simulator; helper response validated against pinned converter | Loopback helper keeps GPL tooling and input font outside the browser bundle; generated glyph raster is not yet drawn by the browser. |
| CFG | Yes for tested corpus | Settings plus raw lines | Partial | Not applicable | Source helper subset | No | Comments, duplicates, unknown keys, whitespace, colons, ordering, and CRLF survive. |
| Unknown tags in new syntax API | Yes | Generic raw tag | No | No | No | No | Unknown names and arguments remain preserved when no generated official definition matches. |
| Malformed syntax in new syntax API | Yes for tested cases | Recovery nodes and diagnostics | No | No | No | No | Incomplete tags, delimiters, and conditionals remain serializable. |
| Conditionals in new syntax API | Yes for tested corpus | Nested tests and branches | Common state plus evidenced `%if`/`%and`/`%or`/`%St` expressions | Selected branch at native pixels | Automatic and manual branch preview | Playback subset included in accepted export | Unknown operands remain preserved and unsupported; false single-branch tests select nothing. |
| Viewports | Yes for tested edits | `%V`, `%Vl`, `%Vi`, `%Vd` | Screen-relative dimensions, font slots, named activation | Integer geometry and clipping | Drag, resize, inspector geometry | Edited viewport accepted by CheckWPS | `%Vl` content renders only when enabled by an active `%Vd`; narrow edits preserve surrounding bytes. |
| Images and preloads | Source reference yes | `%x`, `%xl`, `%xd`, `%X` helpers | Static image, compact preload counts, state frames | Nearest-neighbor pixels plus Rockbox transparency key | Image path and frame fields | Real-theme references exercised | Screen-named asset directories and wrapped archive roots resolve without basename fallback; advanced sprite layouts remain approximate. |
| Album art | Yes | `%Cl`/`%Cd` | Geometry and presence state | Native-pixel rectangle/image | Geometry through source fields | Tested in accepted authored fixture | Missing art uses an explicit placeholder. |
| Progress and volume bars | Yes | `%pb`/`%pv` geometry and named options | Track and volume values | Flat or image/backdrop/slider composition | Geometry and optional path | Authored progress edit/export subset accepted | Gradient and less-common option semantics remain partial. |
| Touch regions | Yes | `%T` geometry/action plus `%Tp`/`%Tl` state | Action retained; recent-touch state is capability-gated | Debug overlay and Level A coordinate input | Geometry and action fields | Tag identity and timeout behavior source-derived | Neither current iPod profile exposes touch; no touchscreen behavior is pretended. |
| Two-way source synchronization | Yes | WPS/SBS/FMS reparsed on apply | WPS subset reinterpreted | Last valid WPS render retained on errors | Source text plus known visual properties | Tested export uses synchronized source | Parser diagnostics show line/column and stale-preview state. |
| Native pixel renderer | Render list retains source links | Not applicable | Operation subset | 320×240 deterministic golden | DOM handles are derived only | Compared with an official 320×240 simulator framebuffer | CSS only zooms the completed canvas; Phase 4 records reproducible pixel hashes, a local diff image, and classified differences. |
| Theme ZIP assets | Yes for authored and local real-theme corpus | Path-safe manifest and one wrapper root | Partial | Partial | Upload bridge | Package contents compared around CheckWPS run | Absolute CFG references resolve inside a conventional outer ZIP folder without weakening case-sensitive path checks. |
| Deterministic export | Yes for tested corpus | Not applicable | Not applicable | Not applicable | Source-aware WPS changes included | Edited Authored Full export accepted | Sorted logical contents and fixed ZIP metadata produce repeatable bytes in fixtures. |

Support terms:

- **Preserved:** Original input survives without unintended changes.
- **Parsed:** The application recognizes source structure.
- **Interpreted:** The application assigns known Rockbox meaning.
- **Rendered:** The browser preview draws an approximation.
- **Editable:** A user-facing edit can update the represented construct.
- **Officially validated:** Behavior has been compared with current Rockbox source tooling.
